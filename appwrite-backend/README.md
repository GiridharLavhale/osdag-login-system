# Appwrite Backend — placeholder

Not built yet. This will implement the same auth + file-access contract as
`custom-backend/`, but backed by Appwrite (Auth, Databases, Storage) instead
of hand-rolled Express + Postgres. Coming next — see `../PROJECT-PLAN.md`.
